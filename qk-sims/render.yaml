# Render Configuration
# This file helps Render understand how to deploy your FastAPI app

# Root directory (backend files are now in root)
root_directory: .

# Build command
build: poetry install --no-root


# Start command  
start: uvicorn main:app --host 0.0.0.0 --port $PORT

# Python version
python_version: 3.13.4